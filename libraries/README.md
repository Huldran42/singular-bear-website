# Libraries

Ce dossier est réservé aux bibliothèques propriétaires, exports de Shader Graph ou paquets tiers qui ne sont pas distribués par npm.

Les dépendances JavaScript restent déclarées dans `package.json` et installées dans `node_modules`. Ne pas copier manuellement une dépendance npm dans ce dossier.
